import { HeadingStyleDirective } from './heading-style.directive';

describe('HeadingStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingStyleDirective();
    expect(directive).toBeTruthy();
  });
});
